export interface ContainerProps {
    type?: 'column' | 'row'
    width?: string
    center?: boolean,
    between?: boolean,
    padding?: boolean
    marginTop?: number,
    marginBottom?: number,
}